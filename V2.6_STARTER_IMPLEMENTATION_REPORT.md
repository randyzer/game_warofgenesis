# GAME_SITE_STARTER v2.6 Stage 2 Implementation Report

Stage: local implementation and verification only.

No commit, push, tag, release, production remote write, frozen v2.5 mutation, GAME_SOP mutation, or Master Prompt mutation was performed.

## 1. Baseline Provenance

Implementation baseline:

- Repository: `https://github.com/randyzer/GAME_SITE_STARTER_BASED_gamesop2.5.git`
- Tag: `starter-v2.5.0`
- Commit: `899cc9ce353e0391fd26c216f0f4b7c419689d86`
- Annotated tag object: `45d353d6a36253fc4d66bf6e340269b47ae2ef20`
- Local source used for copy: `/Users/randyz/work/coding/hot_words_web/Repo_hotgameweb/GAME_SITE_STARTER_BASED_gamesop2.5`

Upstream read-only references:

- GAME_SOP v2.6 tag `GAME_SOP_v2.6`, commit `fd1b3ceee1af51c0347d7766c0f11ee3e34af42f`
- Master Prompt v2.6 tag `codex-master-prompt-v2.6`, commit `4862c52c5b094137079086db4f3e4672691314fe`
- Master Prompt authoritative SHA-256: `b8a7b67c730b12a8c758044b08eb9073e4f66a70ce652082b96d88e368c25734`

Target workspace:

- `/Users/randyz/work/coding/hot_words_web/Repo_hotgameweb/GAME_SITE_STARTER_BASED_gamesop2.6`
- Target remote recorded in request: `https://github.com/randyzer/randyzer-GAME_SITE_STARTER_BASED_gamesop2.6.git`
- Target remote was not written.

## 2. Baseline Copy

The v2.5 starter files were copied into the v2.6 target workspace with generated and repository-control directories excluded:

- Excluded: `.git`, `node_modules`, `dist`, `.astro`
- Preserved existing Stage 1 files already present in the v2.6 workspace
- Frozen v2.5 repository was not modified

## 3. Exact Files Changed, Created, Or Added

Stage artifacts already present or added:

- `V2.6_STARTER_BASELINE_AUDIT.md`
- `V2.6_STARTER_CHANGE_PLAN.md`
- `V2.6_STARTER_IMPLEMENTATION_REPORT.md`

Runtime/config/source changes:

- `game.config.ts`
- `public/logo.svg`
- `scripts/audit-build.ts`
- `scripts/media-validation.ts`
- `scripts/validate-site.ts`
- `src/components/Header.astro`
- `src/config/schema.ts`
- `src/core/brand.ts`
- `src/core/media-readiness.ts`
- `src/styles/global.css`

Test changes:

- `tests/brand.test.ts`
- `tests/html-audit.test.ts`
- `tests/media-audit.test.ts`
- `tests/media-build.test.ts`
- `tests/media-readiness.test.ts`
- `tests/media-rendering.test.ts`
- `tests/navigation.test.ts`

Generated local artifacts from verification:

- `node_modules/`
- `dist/`
- Astro cache files

These generated artifacts are not implementation source changes.

## 4. Root Cause

The v2.5 generated media audit treated every rendered `<img>` as content media. A valid semantic header logo such as `/logo.svg` could therefore fail because it was not a registered `/media/` content asset.

This was a role-boundary defect, not an image accessibility defect. Global HTML accessibility validation already checked image `alt` attributes and remains global.

## 5. Content Media Vs Site Chrome Implementation

Implemented in `scripts/media-validation.ts` and `scripts/audit-build.ts`.

Content media validation now applies to Starter-owned content media output:

- `figure.game-media[data-media-id]`
- fixed page placements such as `data-media-placement="hero"`, `gallery`, and `trailer`
- registered YouTube-nocookie iframe output from validated content media assets
- registered local video output and poster checks

Site chrome images are no longer rejected merely because they are not `/media/` content assets. The broad all-page `<img>` content-media gate was removed.

Global image accessibility remains in `src/core/html-audit.ts`; every `<img>` still must declare `alt`.

## 6. Logo And Header Implementation

Implemented in:

- `src/config/schema.ts`
- `src/core/brand.ts`
- `src/components/Header.astro`
- `src/styles/global.css`
- `game.config.ts`
- `public/logo.svg`

Configuration impact:

- Added one narrow optional config field: `brand.logoPath`
- The field accepts only safe local public image paths.
- No logo alt schema, generic asset role schema, chrome asset manifest, or asset registry was added.

Header behavior:

- If `brand.logoPath` is configured, Header renders a semantic `<img class="brand__logo" src={brandPresentation.logoPath} alt="" aria-hidden="true" decoding="async" />`.
- The home link keeps the accessible name through `aria-label={`${siteConfig.brand.name} home`}`.
- If no logo is configured, Header preserves the existing text mark path.
- Decorative logo alt remains possible through `alt=""`.
- Meaningful brand alt remains possible for chrome validation generally because the chrome audit accepts any explicit `alt`, including non-empty values.

Chrome validation:

- `collectChromeAssetConfigErrors` validates configured logo file existence during `npm run validate`.
- `collectChromeHtmlErrors` validates logo rendering and header home-link naming during generated build audit.

## 7. Media Readiness Implementation

Implemented in:

- `src/core/media-readiness.ts`
- `scripts/validate-site.ts`
- `tests/media-readiness.test.ts`

The readiness check is optional, explicit, and planning-only:

- It reads `MEDIA_DECISION_TABLE.md` only when that file exists.
- It parses a narrow Markdown table shape with `Page ID`, `Route`, `Priority`, `Status`, and optional `Rationale`.
- It keys rows by existing Runtime Page Inventory `pageId` or `route`.
- It does not add media lifecycle fields to Runtime Page Inventory.
- It does not alter the media manifest contract.
- It does not infer priority automatically.

Severity policy implemented:

- Homepage `HIGH PRIORITY` unresolved: blocking validation error
- Non-homepage `HIGH PRIORITY` unresolved: non-blocking warning
- `RECOMMENDED` unresolved: non-blocking informational signal
- `NO MEDIA NEEDED`: valid and non-blocking
- No optional readiness artifact supplied: backward-compatible pass

Planning input is not publication authority. Runtime Page Inventory remains the publication SSOT.

## 8. Stable Core Vs Flexible Edge

Stable Core changes:

- content-media and site chrome role separation
- optional `brand.logoPath` config support
- configured logo existence and generated header rendering validation
- minimal media readiness signal for explicit planning data

Flexible Edge remains outside the Starter:

- YouTube visual fallback UI
- third-party provider fallback handling
- rights review
- media acquisition workflow
- human visual approval
- competitor media benchmark logic
- project-specific media richness decisions

## 9. API And Schema Impact

Additive schema impact only:

- `brand.logoPath?: string`

Unchanged:

- Runtime Page Inventory schema
- media manifest schema
- route-family architecture
- content model
- fact model
- navigation model
- theme token architecture
- `StaticWikiPage` API
- `GameHero` API
- generic page rendering behavior
- deployment model

No HIGH-RISK protected-area change was made.

## 10. Backward Compatibility

Backward-compatible behavior preserved:

- Projects without `brand.logoPath` continue using the existing text mark.
- Projects without `MEDIA_DECISION_TABLE.md` keep existing validation behavior.
- Pages with no content media remain valid.
- `NO MEDIA NEEDED` does not create a hidden quota or imagery requirement.
- Existing `/media/` content assets remain strict and validated.

## 11. Tests Added Or Updated

Logo and chrome/content media tests:

- Semantic header logo does not fail content-media audit.
- Configured logo path is validated as a chrome asset.
- Missing configured logo path is detected.
- Header home link accessible name is validated.
- Invalid article content media image still fails.
- Global `<img alt>` validation still applies to logo and other non-media images.
- Header source boundary renders configured logo while preserving home-link name.
- Text mark presentation remains valid when no logo is configured.

Media readiness tests:

- Homepage `HIGH PRIORITY` resolved passes.
- Homepage `HIGH PRIORITY` unresolved blocks.
- Non-homepage `HIGH PRIORITY` unresolved warns without blocking.
- `RECOMMENDED` unresolved is informational only.
- `NO MEDIA NEEDED` passes without a media quota.
- Missing optional readiness artifact preserves existing pass behavior.
- Narrow Markdown decision table parsing is covered.

Existing media rendering and build tests were updated to distinguish content media images from site chrome logo images.

TDD red check observed before implementation:

- Targeted suite failed with missing `src/core/media-readiness`, unrecognized `brand.logoPath`, broad `<img>` media audit failure, missing chrome helper, and Header lacking logo rendering.

## 12. Verification Results

Environment:

- Node: `v22.16.0`
- npm: `10.9.2`
- Declared package engine: `>=22.19.0 <23 || >=24.0.0`
- Result classification: pass with environment deviation. The engine policy was not changed.

Commands run after implementation:

- `npm run test`
  - Result: PASS
  - Test files: `32 passed (32)`
  - Tests: `360 passed (360)`
- `npm run validate`
  - Result: PASS
  - Output: `Site validation passed: 8 enabled pages, 1 content entries.`
- `npm run check`
  - Result: PASS
  - Astro check: `114 files`, `0 errors`, `0 warnings`, `0 hints`
  - Vitest: `32 passed (32)` test files, `360 passed (360)` tests
- `npm run build`
  - Result: PASS
  - Chain: `npm run validate && astro build && tsx scripts/reconcile-output.ts && pagefind --site dist && tsx scripts/audit-build.ts`
  - Astro static pages built: `8`
  - Reconciliation: `Static output reconciliation passed: 8 inventory routes matched.`
  - Pagefind: `4 pages`, `275 words`
  - Generated build audit: PASS
  - Largest HTML: `10661 B`
  - Largest page CSS reference: `52338 B`
  - Largest page JS reference: `189143 B`
  - Unique referenced CSS: `52338 B`
  - Unique referenced JS: `189143 B`
  - Pagefind output: `652106 B`

## 13. Architecture Principles

High Cohesion:

- Content media rendering and audit rules remain in `src/data/media/*`, `src/components/media/*`, and `scripts/media-validation.ts`.
- Chrome logo checks are narrow helpers in `scripts/media-validation.ts` and build/validate aggregation scripts.
- Media readiness planning signals live in `src/core/media-readiness.ts`, separate from renderers and publication inventory.

Low Coupling:

- `brand.logoPath` is consumed by `src/core/brand.ts` and `src/components/Header.astro` without coupling logos to `media.json`.
- Readiness decisions reference existing `pageId` or `route`; they do not mutate Page Inventory.
- Build audit composes chrome, HTML accessibility, and content media validators without merging their responsibilities.

Modular Design:

- `scripts/validate-site.ts` remains the CLI aggregation point.
- `scripts/audit-build.ts` remains generated-output audit aggregation.
- `src/core/media-readiness.ts` exposes reusable pure parsing and signal functions.
- Header remains a presentation component fed by config-derived brand presentation.

Adaptable Design:

- Text-only branding remains valid.
- Configured semantic logo rendering is supported.
- No-media pages remain valid.
- Projects can opt into media readiness by adding the planning artifact.

Stable Core / Flexible Edge:

- Stable Core receives only repeated technical boundary fixes and the lightweight explicit planning signal.
- Provider-specific fallback visuals and media workflow decisions remain project-edge patterns.

## 14. Deliberate Non-Changes

Not implemented:

- YouTube or third-party media fallback UI
- provider adapters
- generic iframe fallback
- provider registry
- media embed framework
- `ChromeAssetRegistry`
- `AssetRole` schema
- chrome asset manifest
- generic asset registry
- media lifecycle state in Runtime Page Inventory
- rights status logic
- Human Risk Accepted logic
- competitor benchmark logic
- Human visual approval logic
- acquisition workflow
- quota system
- CMS, DB, DAM, auth, backend, or page builder

## 15. YouTube Fallback Status

Status: DEFER / PROJECT-EDGE PATTERN.

Reason: v2.6 Stable Core does not yet have repeated production evidence that a reusable third-party fallback UI belongs in the Starter. Existing validated YouTube-nocookie embed behavior remains unchanged.

## 16. Regression Expectations

Future regressions should be caught when:

- a header logo is misclassified as content media;
- configured logo paths do not exist;
- the header logo fails to render in generated output;
- the header home link loses its accessible name;
- a content media image points at a missing `/media/` file;
- global image `alt` validation is weakened;
- a homepage high-priority unresolved media decision is allowed to pass;
- non-homepage high-priority unresolved media is accidentally promoted to a universal blocking quota;
- missing `MEDIA_DECISION_TABLE.md` stops being backward-compatible.

## 17. Targeted Parser Correctness Repair

Follow-up review found one required pre-freeze issue: when `MEDIA_DECISION_TABLE.md` existed but was malformed, the parser could return an empty decision set with no errors. That failed open and could hide homepage `HIGH PRIORITY` unresolved media.

Narrow repair:

- `parseMediaDecisionTableMarkdown` now returns `{ decisions, errors }`.
- Required columns are exactly `Priority`, `Status`, and at least one locator column: `Page ID` or `Route`.
- Accepted `Priority` values remain `HIGH PRIORITY`, `RECOMMENDED`, and `NO MEDIA NEEDED`.
- Accepted `Status` values remain `resolved` and `unresolved`.
- A present empty file, malformed table structure, missing required columns, unsupported enum values, or a non-empty row without `Page ID`/`Route` now produces deterministic parse errors.
- `scripts/validate-site.ts` surfaces parse errors through the existing validation error path.
- Absence of `MEDIA_DECISION_TABLE.md` remains backward-compatible and valid.

No scope expansion was made:

- No new dependency.
- No rights, waiver, Game Recognition, visual approval, acquisition, publication-state, or complete lifecycle parsing.
- No Runtime Page Inventory, media manifest, Header, logo, YouTube, engine, or package-contract change.

Targeted evidence:

- `npm run test -- tests/media-readiness.test.ts`: PASS, `1` file, `13` tests.
- Temporary-project `npm run validate` with missing `Status` column: failed with `MEDIA_DECISION_TABLE.md is missing required column: Status.`
