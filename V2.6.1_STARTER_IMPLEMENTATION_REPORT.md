# GAME_SITE_STARTER V2.6.1 Phase C Implementation Report

## Verdict

REPAIR COMPLETE — V2.6.1 P2 RELEASE HYGIENE READY FOR CROSS-REPO RE-REVIEW

Current status (2026-09-08): `READY FOR FULL CROSS-REPO SEMANTIC RE-REVIEW`.
Cross-repo P1 targeted re-review = PASS; Human Approval of P1 repair = PASS,
as supplied by the Human-owned disposition for this task. P2-1 is ACCEPTED /
DEFERRED for V2.6.1; P2-2 and P2-3 are repaired and validated under Node
`v22.22.0`. See the appended P2 Release-Hygiene Narrow Repair section for the
current evidence and exact next step. This is repair-author evidence, not full
cross-repo review PASS or final Human Release Approval.

## Historical Verdict — Cross-Repo P1 Author Snapshot

The following P1 author verdict and the earlier sections are preserved as
chronology. The current verdict above and appended P2 section supersede their
pending-review, P2 disposition, and next-step statements.

REPAIR COMPLETE — V2.6.1 CROSS-REPO P1 FINDINGS READY FOR TARGETED RE-REVIEW

Current status (2026-09-08): `READY FOR CROSS-REPO FINDINGS TARGETED RE-REVIEW`. The two P1 findings from the cross-repo semantic review have been narrowly repaired and validated under Node `v22.22.0`. This is fix-author evidence, not cross-repo review PASS or Human Approval for these fixes. See the appended Cross-Repo P1 Narrow Repair section for current evidence and next step.

## Historical Verdict — Round 3 Author Snapshot

The following original Phase C / Round 1–3 sections, including their findings and next-step statements, are preserved as chronology. The appended cross-repo repair section supersedes their current-status statements without rewriting prior review or approval evidence.

REPAIR COMPLETE — GAME_SITE_STARTER V2.6.1 READY FOR TARGETED RE-REVIEW ROUND 3

Implementation work for D1-D3, the first targeted-review repairs, the second narrow D1 repair, and the third narrow D1 repair is complete in source, tests, docs, and build output. The first validation attempt used Node `v22.16.0`, which was below the intended runtime and did not close target-runtime validation. Runtime closure was then completed under Node `v22.22.0`. The first targeted review, targeted re-review, and targeted re-review round 2 all returned `REVISE`; this report preserves that history and does not claim targeted re-review round 3 PASS, Human Approval, final release PASS, frozen release, commit, push, tag, or deployment.

## Baseline Provenance

- Starter workspace: `/Users/randyz/work/coding/hot_words_web/Repo_hotgameweb/GAME_SITE_STARTER_BASED_gamesop2.6.1`
- Baseline repository requested: `https://github.com/randyzer/GAME_SITE_STARTER_BASED_gamesop2.6.git`
- Current remote: `https://github.com/randyzer/GAME_SITE_STARTER_BASED_gamesop2.6.1.git`
- Frozen tag: `starter-v2.6.0`
- Frozen commit: `8f54c0844408e5eff4be2e456e88d11f68b69937`
- Annotated tag object: `f144c7ee081a667de4a9bb190b9b1458dec9fd4a`
- Current HEAD before Phase C work: `8f54c0844408e5eff4be2e456e88d11f68b69937`
- Current branch: `main`

## Pre-Existing Packaging Changes

Baseline inspection found only approved packaging/identity changes before Phase C implementation:

- `README.md`
- `docs/CONTENT_AND_DATA_GUIDE.md`
- `docs/QA_CHECKLIST.md`
- `public/media/README.md`
- `tests/release-identity.test.ts`
- `V2.6_STARTER_IMPLEMENTATION_REPORT.md`
- Archive move of `V2.6_STARTER_BASELINE_AUDIT.md` and `V2.6_STARTER_CHANGE_PLAN.md` into `archive/v2.6/`

No pre-existing `src/`, `scripts/`, runtime config, dependency, or lockfile Phase C implementation was present.

## Files Changed

PHASE_C_IMPLEMENTATION:

- `src/core/media-readiness.ts`
- `scripts/validate-site.ts`
- `src/core/public-sources.ts`
- `src/components/wiki/Sources.astro`
- `src/components/ToolShell.astro`
- `src/core/tool-definitions.ts`
- `src/components/islands/CalculatorIsland.tsx`
- `src/components/islands/PlannerIsland.tsx`
- `tests/media-readiness.test.ts`
- `tests/player-facing-metadata.test.ts`
- `tests/public-sources.test.ts`
- `tests/tool-definitions.test.ts`
- `tests/tool-island-boundary.test.ts`
- `tests/runtime-contract.test.ts`
- `vitest.config.ts`
- `package.json`
- `package-lock.json`
- `README.md`
- `docs/CONTENT_AND_DATA_GUIDE.md`
- `docs/QA_CHECKLIST.md`
- `public/media/README.md`
- `tests/release-identity.test.ts`
- `V2.6.1_STARTER_IMPLEMENTATION_REPORT.md`

APPROVED_PREEXISTING_PACKAGING:

- `V2.6_STARTER_IMPLEMENTATION_REPORT.md`
- `archive/v2.6/V2.6_STARTER_BASELINE_AUDIT.md`
- `archive/v2.6/V2.6_STARTER_CHANGE_PLAN.md`
- deletion entries for root `V2.6_STARTER_BASELINE_AUDIT.md` and `V2.6_STARTER_CHANGE_PLAN.md`

UNRELATED: none.

UNKNOWN: none.

## D1 Implementation

- Added `docs/MEDIA_DECISION_TABLE.md` authority support.
- Preserved root `MEDIA_DECISION_TABLE.md` as legacy compatibility.
- Added duplicate-authority fail-closed behavior when both paths exist.
- Preserved no-table compatibility as no readiness signal.
- Replaced old `Priority`/`Status` projection with V2.6.1 lifecycle fields.
- Enforced exact lifecycle enum values without fuzzy matching.
- Enforced the exact canonical SOP header schema without aliases, unknown columns, or duplicate columns.
- Added exact header/separator/data-row width checks and deterministic malformed-row errors.
- Separates exact canonical columns from unconditionally required row values; `notes` remains a canonical column but is not universally required.
- Requires non-empty `notes` for every SOP-defined no-media exception, including `WAIVED` no-media rows. In the separate media-needed branch, a Human waiver explanation remains satisfied by non-empty `notes` or `owner_or_defer_reason`; non-waived rights-risk and fallback-only states require notes.
- Rejects structurally present all-empty table data rows with a deterministic malformed-row error while preserving blank lines outside the table.
- Resolves `pageId_or_route` against Runtime Page Inventory before projection.
- Fails on zero match, multiple matches, and duplicate rows resolving to the same Runtime Inventory page.
- Applies ordered SOP-derived projection branches for `NO MEDIA NEEDED` and media-needed rows.
- Keeps Human waiver ownership narrow: validates exact state plus explanation; does not judge rights or legal quality.
- Keeps homepage `HIGH PRIORITY` unresolved media blocking while guide-page unresolved high-priority media remains warning-level.

## D1 Authority-Path Behavior

- Only `docs/MEDIA_DECISION_TABLE.md`: PASS, accepted.
- Only root `MEDIA_DECISION_TABLE.md`: PASS, accepted as legacy compatibility.
- Neither table: PASS, V2.6 no-table compatibility preserved.
- Both tables: PASS, deterministic validation error.

## D1 Enum / Combination Validation

Covered fail-closed cases:

- malformed table/header
- missing required column
- unknown enum
- malformed non-empty row
- unsupported exact state combination
- invalid waiver without explanation
- fallback-only without waiver not resolved
- `NO MEDIA NEEDED` missing positive rationale
- conflicting `NO MEDIA NEEDED` states

## D1 Runtime Inventory Resolution

Runtime Page Inventory remains the publication SSOT. Media table rows cannot create routes, change publication, or infer page identity. `pageId_or_route` must match exactly one inventory `pageId` or `route`.

## D2 Public/Internal Boundary

- Added `projectPublicSources()` as a narrow public-safe projection.
- Public renderers now consume URL, hostname, source type label, accessed date, and published date only.
- `evidenceNote` remains in internal source data and schemas.
- `wiki/Sources.astro` and `ToolShell.astro` no longer render `evidenceNote`.
- Client island tool logic no longer imports the full tool schema module, preventing internal schema field names from entering the public JS bundle.
- `ToolShell.astro` now projects calculator and planner definitions into explicit minimal client payloads before `client:load` serialization.
- Calculator formula nodes, inputs, planner slots, and planner options are copied through deep allowlists; internal definition metadata and raw sources do not cross the island boundary.

## D3 Node Contract

- `.nvmrc`: `22.22.0`
- `package.json` engines: `>=22.19.0 <23`
- `package-lock.json` root engines: `>=22.19.0 <23`
- CI continues to use `node-version-file: .nvmrc`
- Active docs no longer claim Node 24 compatibility.

## Runtime Evidence

### Initial Validation Attempt

- Intended Node version: `22.22.0`
- Actual Node version used by current process: `v22.16.0`
- Actual npm version: `10.9.2`
- Available version managers in current process: none found for `nvm`, `fnm`, `asdf`, or `mise`
- Available Node binary: `/usr/local/bin/node`

Runtime validation under intended Node `22.22.0`: INCOMPLETE / NOT CLOSED during the first validation attempt.

All initial PASS results in this attempt were produced under actual Node `v22.16.0`, not intended Node `22.22.0`.

### Runtime Closure

Environment discovery then found:

- `nvm` was already installed.
- Node `v22.22.0` was already installed.
- The active shell had been resolving `/usr/local/bin/node` as Node `v22.16.0`.

Existing Node `v22.22.0` was activated via `nvm`.

- Intended Node version: `22.22.0`
- Actual Node version: `v22.22.0`
- Node path: `/Users/randyz/.nvm/versions/node/v22.22.0/bin/node`
- npm path: `/Users/randyz/.nvm/versions/node/v22.22.0/bin/npm`
- `.nvmrc`: `22.22.0`
- `package.json` engines.node: `>=22.19.0 <23`

Runtime validation under intended Node `22.22.0`: PASS / CLOSED.

## Dependencies / Lockfile

- Existing package manager contract: npm with `package-lock.json`.
- Initial `npm ci`: PASS, installed dependencies from the committed lockfile under Node `v22.16.0`.
- Initial `npm ci` emitted EBADENGINE warnings because Node `v22.16.0` is below `>=22.19.0`.
- Runtime-closure `npm ci` under Node `v22.22.0`: PASS.
- Runtime-closure EBADENGINE warnings: NONE.
- No dependency additions.
- Lockfile dependency tree unchanged; only root `engines.node` metadata changed to align the Node 22-only contract.
- `node_modules` remains ignored.

## Validation Results

- Initial targeted D1/D2/D3 tests under Node `v22.16.0`: PASS, 4 files / 18 tests.
- Initial release identity test under Node `v22.16.0`: PASS, 1 file / 1 test.
- Initial full test suite under Node `v22.16.0`: PASS, 34 files / 368 tests.
- Initial `npm run validate` under Node `v22.16.0`: PASS, 8 enabled pages, 1 content entry.
- Initial `npm run check` under Node `v22.16.0`: PASS, Astro check 0 errors / 0 warnings / 0 hints; Vitest 34 files / 368 tests.
- Initial `npm run build` under Node `v22.16.0`: PASS, 8 pages built; output reconciliation passed; Pagefind built; generated build audit passed.
- Runtime-closure Phase C targeted tests under Node `v22.22.0`: PASS, 10 files / 89 tests.
- Runtime-closure full test suite under Node `v22.22.0`: PASS, 34 files / 368 tests.
- Runtime-closure `npm run validate` under Node `v22.22.0`: PASS, 8 enabled pages / 1 content entry.
- Runtime-closure `npm run check` under Node `v22.22.0`: PASS, Astro 0 errors / 0 warnings / 0 hints; Vitest 368 PASS.
- Runtime-closure `npm run build` under Node `v22.22.0`: PASS.
- `git diff --check`: PASS.
- Targeted-repair tests under Node `v22.22.0`: PASS, 5 files / 42 tests.
- Targeted-repair full suite under Node `v22.22.0`: PASS, 35 files / 388 tests.
- Targeted-repair `npm run validate`: PASS, 8 enabled pages / 1 content entry.
- Targeted-repair `npm run check`: PASS, Astro 118 files with 0 errors / 0 warnings / 0 hints; Vitest 35 files / 388 tests.
- Targeted-repair `npm run build`: PASS, 8 pages built; output reconciliation, Pagefind, and generated build audit passed.
- D1 repair round 2 targeted test under Node `v22.22.0`: PASS, 1 file / 33 tests.
- D1 repair round 2 plus D2 narrow regression tests under Node `v22.22.0`: PASS, 5 files / 46 tests.
- D1 repair round 2 full suite under Node `v22.22.0`: PASS, 35 files / 392 tests.
- D1 repair round 2 `npm run validate`: PASS, 8 enabled pages / 1 content entry.
- D1 repair round 2 `npm run check`: PASS, Astro 118 files with 0 errors / 0 warnings / 0 hints; Vitest 35 files / 392 tests. Two existing empty-directory glob-loader notices were emitted outside Astro diagnostics.
- D1 repair round 2 `npm run build`: PASS, 8 pages built; 8 Runtime Page Inventory routes reconciled; Pagefind indexed 4 pages; generated build audit passed for 8 pages.
- D1 repair round 3 RED test under Node `v22.22.0`: expected FAIL, 1 failed / 34 passed; the no-media `WAIVED` blank-notes fixture reproduced the remaining P1.
- D1 repair round 3 targeted D1 test under Node `v22.22.0`: PASS, 1 file / 35 tests.
- D1 repair round 3 production parser/readiness probes under Node `v22.22.0`: PASS, 9 / 9 probes.
- D1 repair round 3 plus D2 narrow regression tests under Node `v22.22.0`: PASS, 5 files / 48 tests.
- D1 repair round 3 full suite under Node `v22.22.0`: PASS, 35 files / 394 tests.
- D1 repair round 3 `npm run validate`: PASS, 8 enabled pages / 1 content entry.
- D1 repair round 3 `npm run check`: PASS, Astro 118 files with 0 errors / 0 warnings / 0 hints; Vitest 35 files / 394 tests. Two existing empty-directory glob-loader notices were emitted outside Astro diagnostics.
- D1 repair round 3 `npm run build`: PASS, 8 pages built; 8 Runtime Page Inventory routes reconciled; Pagefind indexed 4 pages; generated build audit passed for 8 pages.
- D1 repair round 3 `git diff --check`: PASS.

The first validation preserved above did not close target-runtime validation because it ran under actual Node `v22.16.0`. The later runtime-closure validation under actual Node `v22.22.0` closes the target-runtime evidence gap.

## Public Build Output Regression Check

- Runtime-closure Astro build output: PASS, 8 pages built.
- Static output reconciliation: PASS, matched 8 Runtime Page Inventory routes.
- Pagefind HTML discovery: PASS, found 8 HTML files.
- Pagefind indexing: PASS, indexed 4 pages.
- Build audit: PASS for 8 pages.
- D1 adoption smokes: PASS.
- D2 public leak recheck: PASS.
- D1 repair round 3 D2 narrow regression suite: PASS, 4 D2-related files plus the D1 test file / 48 total tests.
- `dist` leak scan: PASS, no `evidenceNote` or representative internal workflow/media lifecycle markers found.
- Public source fields still render: PASS, guide source block renders host/type/date/link.
- No unexpected public pages observed.
- Homepage no-media hero behavior remains present: `game-hero--no-media` present.
- Homepage media hero markers remain absent: `game-hero--has-media` absent; `game-hero__media` absent.
- Enabled calculator ToolShell render fixture: PASS; the unique internal evidence marker is absent from rendered HTML and Astro serialized island props.
- Targeted-repair `dist` scan: PASS; no `INTERNAL_TOOL_EVIDENCE_LEAK_MARKER` or `evidenceNote` match.

## Targeted Reviews And Narrow Repairs

- Targeted review verdict: `REVISE - GAME_SITE_STARTER V2.6.1 PHASE C TARGETED REVIEW`.
- Targeted review findings: 3 P1 findings covering D1 parser fail-open behavior, the D2 tool-island hydration boundary, and this report's overstated D1/D2 closure.
- First narrow repair: alias headers, unknown/duplicate columns, malformed header/row widths, and empty required source fields were made fail-closed; explicit client-safe tool projections closed the D2 hydration boundary; this report preserved the first `REVISE` verdict.
- Targeted re-review verdict: `REVISE - GAME_SITE_STARTER V2.6.1 PHASE C TARGETED RE-REVIEW`.
- Targeted re-review result: D2 was independently CLOSED. D1 remained open because `notes` was incorrectly required for every row and an all-empty structural table row could still be skipped; the report therefore remained open for overstating closure.
- Second narrow D1 repair: canonical columns and unconditionally required values are now separate; ordinary approved-integrated rows may omit notes; owner-explained Human waivers may omit notes; SOP-defined no-media, rights-risk, and non-waived fallback-only states were intended to require notes; all-empty structural rows fail closed.
- Round 2 production-path probes cover optional notes, required rationale, all-empty structural rows, blank lines outside tables, alias headers, extra cells, empty provenance, owner-only waiver, and rights-risk notes.
- Targeted re-review round 2 verdict: `REVISE — GAME_SITE_STARTER V2.6.1 PHASE C TARGETED RE-REVIEW ROUND 2`.
- Targeted re-review round 2 result: Finding B and D2 remained closed, but D1 retained one P1 because a blanket `WAIVED` exemption allowed `NO MEDIA NEEDED` rows with blank notes to pass.
- Third narrow D1 repair: conditional notes validation now branches on `media_need` first. Every no-media exception requires notes; the owner-or-notes waiver rule remains scoped to `HIGH PRIORITY`, `RECOMMENDED`, and `OPTIONAL` rows.
- Round 3 regression coverage adds no-media `WAIVED` blank-notes rejection and valid-notes acceptance, preserves the media-needed owner-only waiver positive case, and directly verifies that ordinary pending media-needed rows may keep optional notes blank.
- Current status: `READY FOR TARGETED RE-REVIEW ROUND 3`.

## Deferred Items

- Generic YouTube fallback system: DEFERRED as requested.
- D7 tap-target/responsive hardening: DEFERRED as requested.
- SOP/Master Prompt edits: DEFERRED / NOT TOUCHED.
- Targeted review and Human approval: DEFERRED to the next gate.

## Findings

- P0 findings: none in implemented source.
- Original implementation self-assessment before targeted review: P1 findings none.
- Independent targeted review found 3 P1 findings and triggered the first narrow repair.
- Independent targeted re-review closed D2 but kept D1 and report accuracy open, including one new P1 false-negative caused by unconditional `notes` validation.
- Independent targeted re-review round 2 found one P1: the blanket `WAIVED` exemption crossed into the no-media branch and allowed blank notes.
- D1 repair round 3 self-check: the remaining P1 is repaired, no P1 finding remains open in the narrow repaired scope, and independent targeted re-review round 3 is still required for closure.
- P2 findings: none.

## Commits / Pushes / Tags / Deployments

- Commits: none.
- Pushes: none.
- Tags: none.
- Deployments: none.

## Exact Next Step

Run independent Phase C targeted re-review round 3 against the third narrow D1 repair and preserved D2 evidence. Do not claim targeted re-review round 3 PASS, Human Approval, final release PASS, frozen release, commit, push, tag, or deployment until those gates are explicitly completed.

## Cross-Repo P1 Narrow Repair — 2026-09-08

### Review Source And Scope

- Cross-Repo Semantic Review = `REVISE — V2.6.1 CROSS-REPO SEMANTIC REVIEW`, with P0 = 0, P1 = 2, P2 = 3.
- Review source read in full: `../V2.6.1_PATCH/V2.6.1_CROSS_REPO_SEMANTIC_REVIEW.md`, especially findings in section 34, contract/semantic matrices, and reproduction commands in appendices B/C.
- Semantic authority: `../GAME_SOP_V2.6.1/docs/NEW_GAME_SITE_SOP_v2.6.1.md`; canonical full template: `../GAME_SOP_V2.6.1/docs/MEDIA_DECISION_TABLE_TEMPLATE.md`.
- Master authority checked for these two behaviors: `../GAME_CODEX_MASTER_PROMPT_V2.6.1/CodexMasterPrompt_v2.6.1Final.md`, section 12 / 12.1.
- Prior Starter Round 3 PASS and prior Human Approval are accepted as the supplied historical state; they do not approve the new cross-repo fixes.
- Execution workspace and repository root: `/Users/randyz/work/coding/hot_words_web/Repo_hotgameweb/GAME_SITE_STARTER_BASED_gamesop2.6.1`.
- Initial HEAD matched `8f54c0844408e5eff4be2e456e88d11f68b69937`; initial staging was empty. Existing worktree changes were preserved.

### P1-A — Waiver / Pending Evaluation Order

Original finding: explained `WAIVED` + visual `PENDING` rows were prematurely blocked. The original `if (humanDecision === "WAIVED")` entered a terminal branch even when the complete SOP waiver combination did not match. It returned a misleading missing-explanation error despite recorded explanations.

Previous media-needed order: explicit Human/visual REVISE/BLOCK → any WAIVED (resolve only with visual PASS and explanation; otherwise error) → approved integrated media → pending → fallback-only → unsupported.

New media-needed order: explicit Human/visual REVISE/BLOCK → complete WAIVED + visual PASS + recorded explanation → approved integrated media → pending → fallback-only → unsupported. Schema/enums and Inventory identity are still validated before branch projection. The separate NO MEDIA NEEDED branch is unchanged.

SOP authority lines 654–684 require the first complete matching rule. WAIVED/PENDING does not match waiver rule 2; it proceeds to pending rule 4. It is not automatically resolved. Human waiver ownership, branch scope, required fields, and explicit REVISE/BLOCK precedence remain intact.

### P1-B — Full-Template Table Selection

Original finding: the parser selected the first Markdown header followed by a separator. In the official template, that is the three-column explanatory projection table; the canonical lifecycle table occurs later under Route Decisions.

The parser now scans candidate headers and matches the exact ordered 13-column canonical schema using the existing cell parser. No alias, subset, or fuzzy matching is used. Selection is independent of physical first-table position and section title.

- Zero canonical matches: an existing file fails with a deterministic error. Empty files and files without a header/separator retain their existing malformed-file errors. When noncanonical candidate tables exist, the parser reports no canonical lifecycle table and retains specific header diagnostics; it never parses those candidates as lifecycle data.
- Exactly one canonical match: parse that table with the existing strict separator/data-width, required-value, enum, and conditional-notes checks. Original document line numbers are preserved.
- Multiple canonical matches: fail closed with duplicate canonical authority and header line numbers before parsing decisions.
- File absence remains `source: none`; docs/root duplicate-path detection is unchanged.
- The regression fixture is the complete local SOP template with only its placeholder Route Decisions row replaced by an explained WAIVED/PASS guide row. Both preceding explanatory tables, prose, checklists, and release notes remain. Tests do not require a sibling SOP repository at runtime.

### Production Probes

All ten requested parser/readiness probes passed under Node v22.22.0:

| Probe | Actual outcome |
|---|---|
| 1. Explained WAIVED + visual PENDING | RECOMMENDED guide: info / pending. Targeted tests also verify HIGH PRIORITY home error, HIGH PRIORITY guide warning, and RECOMMENDED/OPTIONAL home/guide info; all carry the pending reason. |
| 2. Explained WAIVED + visual PASS | Resolved waiver; no readiness signals. |
| 3. WAIVED + visual REVISE | Explicit blocking error, not waiver or pending. |
| 4. WAIVED + visual BLOCK | Explicit blocking error, not waiver or pending. |
| 5. WAIVED with missing explanation | Parse error requiring owner_or_defer_reason; zero parsed decisions. PASS and PENDING visual variants are covered in tests. |
| 6. Explanatory table before canonical | One canonical decision selected and resolved. |
| 7. Multiple explanatory tables before canonical | One canonical decision selected and resolved; filled full SOP template also passes. |
| 8. Existing file with no canonical table | Deterministic parse error; zero decisions; no silent no-table compatibility. |
| 9. Two canonical tables in one file | Duplicate canonical authority error; zero decisions. |
| 10. Canonical-only table | One decision parsed and resolved. |

Actual `scripts/validate-site.ts` was also executed in temporary project directories using the real Starter source/public inputs. Eight scenarios passed their expected exit-code assertions: A docs-only = 0; B root legacy-only = 0; C neither = 0; D both paths = 1; filled full SOP template = 0; RECOMMENDED guide WAIVED/PENDING = 0 with pending info; existing noncanonical-only file = 1; duplicate canonical tables in one file = 1. Temporary directories were removed; project authority files were not added to the Starter root or docs.

### Semantic Matrix Recheck

The exact Node script in review Appendix C was extracted and executed without changing its oracle or comparison domain, both before and after repair. It calls the production parser/readiness functions and compares resolved/error/warning/info for all 2,880 exact lifecycle combinations on each of home and guide. All explanation fields are fixed non-empty; this comparison does not judge Human explanation quality.

| Run | Comparisons | Parse errors | Severity differences |
|---|---:|---:|---:|
| Before repair | 5,760 | 0 | 180 |
| After repair | 5,760 | 0 | 0 |

Before repair the five groups matched the review exactly: home RECOMMENDED/OPTIONAL and guide HIGH PRIORITY/RECOMMENDED/OPTIONAL, 36 WAIVED/PENDING mismatches each. After repair there are no remaining differences to classify as EXPECTED / OUT-OF-DOMAIN or SEMANTIC DRIFT. Remaining P1-level semantic drift in the compared domain = 0. The additional HIGH PRIORITY homepage tests verify the pending reason even where the old and new severity are both error.

### Regression And Runtime Evidence

All tests and probes in this repair used `PATH=/Users/randyz/.nvm/versions/node/v22.22.0/bin:$PATH` or the absolute same Node binary. Intended Node = v22.22.0; actual `node -v` = v22.22.0; `which node` = `/Users/randyz/.nvm/versions/node/v22.22.0/bin/node`. Initial read-only discovery found v22.16.0 at `/usr/local/bin/node`; no tests were executed under that default runtime. No installs or shell startup changes were made.

| Command / evidence | Actual result |
|---|---|
| `npm test -- tests/media-readiness.test.ts` before production repair | Expected RED: 1 file, 13 failed / 43 passed, 56 total. Includes both P1 counterexamples and new selection diagnostics. |
| `npm test -- tests/media-readiness.test.ts` after repair | PASS: 1 file / 56 tests. |
| Ten explicit production probes | PASS: 10 / 10. |
| Actual validator adoption / template / malformed-file scenarios | PASS: 8 / 8 expected exit codes. |
| `npm test` | PASS: 35 files / 415 tests, up from prior 35 / 394. |
| `npm run validate` | PASS: 8 enabled pages / 1 content entry. |
| `npm run check` | PASS: Astro 118 files, 0 errors / 0 warnings / 0 hints; Vitest 35 files / 415 tests. |
| `npm run build` | PASS: 8 pages built; 8 Inventory routes reconciled; Pagefind indexed 4 pages; generated build audit passed for 8 pages. |
| `git diff --check` | PASS. |

Check/build emitted the two existing empty-directory glob-loader notices for `src/content/meta` and `src/content/news`, outside Astro diagnostics.

D1 previous closures remain covered and passing: canonical/alias/unknown/duplicate/missing headers, row and separator widths, required provenance, all-empty structural rows, harmless outside-table blank lines, exact enums, Inventory zero/unique/ambiguous identities, duplicate decision rows, no-media rationale/conflict/waived-notes rules, branch-scoped waiver, ordinary Human/visual pending, approved integration, unsupported states, and adoption A/B/C/D.

D2 remains CLOSED and its code is unchanged. The full suite includes all 4 existing D2 test files / 13 tests, covering internal evidence retention, public source projection, client payloads, and actual ToolShell hydration output. A separate production-function probe injected internal markers into calculator inputs/nested formula nodes and planner slots/options; deep allowlists removed them from client payloads while originals retained evidence. Fresh `dist` scanning found no `evidenceNote`, `INTERNAL_TOOL_EVIDENCE_LEAK_MARKER`, or `INTERNAL_EVIDENCE_NOTE_MARKER` matches.

### Targeted Contract Alignment

| Finding | SOP | Master | Starter after repair | Result |
|---|---|---|---|---|
| P1-A | Authority 654–684: exact branch rules; waiver requires visual PASS; otherwise continue to pending; REVISE/BLOCK first. | Section 12, 445–472: inherits SOP exact order, first matching outcome, Human-owned waiver and separate no-media branch. | Complete waiver predicate; pending fallthrough; no-media and explicit blocking preserved. | ALIGNED |
| P1-B | Authority 606–622 and full template 79–105: exact 13-column lifecycle schema coexists with preceding explanatory tables. | Section 12 / 12.1: use SOP schema and supported table; forbid aliases and duplicate path authority. | Unique exact canonical header selects lifecycle data; zero/multiple matches fail closed; full filled template and all path scenarios verified. | ALIGNED |

This is the requested fix-author comparison of the repaired contracts, not an independent targeted review or a new cross-repo re-review. SOP and Master were not modified to obtain alignment.

### Incremental Diff Classification And Deferred Findings

Compared with the saved pre-repair worktree contents, this task changes only:

| File | Classification |
|---|---|
| `src/core/media-readiness.ts` | P1_A_EVALUATION_ORDER + P1_B_TABLE_SELECTION |
| `tests/media-readiness.test.ts` | TEST |
| `tests/fixtures/media/media-decision-table-sop-v2.6.1.md` | TEST |
| `V2.6.1_STARTER_IMPLEMENTATION_REPORT.md` | REPORT |

UNRELATED = 0; UNKNOWN = 0. No generic parser framework, new subsystem, dependency, publication authority, lifecycle storage, or architecture drift was introduced. `scripts/validate-site.ts` required no changes.

- P0 findings in this repaired scope: 0.
- P1 findings remaining in this repaired scope: 0 by fix-author validation; independent targeted re-review is still required for closure.
- P2-01 duplicate page identity parsing: `DEFERRED P2 — unchanged by this repair`.
- P2-02 npm package version / release identity explanation: `DEFERRED P2 — unchanged by this repair`.
- P2-03 EntityDatabase/internal implementation wording: `DEFERRED P2 — unchanged by this repair`.
- All pre-existing tracked/untracked changes remain. Starter staging remains empty; HEAD unchanged. Content/index snapshot comparison confirms SOP and Master unchanged, and the cross-repo review artifact unchanged.
- Commits = 0; pushes = 0; tags = 0; deployments = 0. No staging, release packaging, freeze, or cross-repo re-review was started.

### Current Status And Exact Next Step

`READY FOR CROSS-REPO FINDINGS TARGETED RE-REVIEW`.

Next: independently review the changed evidence for P1-A and P1-B, then obtain PASS and Human Approval through the established closure process. Disposition the three deferred P2s after P1 closure. This task stops here; it does not start targeted/cross-repo re-review, claim cross-repo review PASS, approve these fixes, or enter release packaging.

## P2 Release-Hygiene Narrow Repair — 2026-09-08

### Human-Owned Disposition And Scope

- Cross-repo P1 targeted re-review: PASS, supplied by the user for this task.
- Human Approval of P1 repair: PASS, supplied by the user for this task.
- P2-1 duplicate Runtime Page Inventory identity parsing: **ACCEPTED / DEFERRED P2 for V2.6.1**. This is non-blocking maintainability debt with no current semantic/publication-authority failure. No implementation or test for P2-1 was changed; the report only records the disposition.
- P2-2 version identity clarification: repaired.
- P2-3 unintended player-facing implementation wording: repaired.
- P0 = 0; P1 = 0 in this authorized repair scope, carrying forward the supplied P1 closure. Remaining P2 = 1 accepted/deferred finding (P2-1); P2-2/P2-3 await the next independent review of this repair evidence.

Workspace and Git root both resolve to `/Users/randyz/work/coding/hot_words_web/Repo_hotgameweb/GAME_SITE_STARTER_BASED_gamesop2.6.1`.
Initial HEAD matched `8f54c0844408e5eff4be2e456e88d11f68b69937`; the index was empty of staged changes. Initial status had 21 tracked changed/deleted paths and 8 untracked files (including the two files under `archive/`). These existing changes were preserved. No reset, clean, stash, restore, installation, or shell startup edit was performed.

### P2-2 — Evidence And Clarification

Original finding: active docs claimed Starter v2.6.1 while `package.json` and both root version fields of `package-lock.json` contained `0.1.0`, without explaining their relationship.

Repository evidence supports separate identities:

- Current `package.json` has `name: game-site-starter`, `private: true`, and `version: 0.1.0`; lockfile top-level and root-package versions agree.
- `git show 8f54c0844408e5eff4be2e456e88d11f68b69937:package.json` confirms the V2.6 baseline already retained the same private package version.
- Historical `docs/STARTER_V2.5_CHANGE_PROPOSAL.md:18` records package version `0.1.0` alongside README identity `Game Wiki Starter 2.0`; its later baseline record at line 894 also retains `0.1.0`.
- Current README, content guide, QA checklist, media README, and release-identity test identify Starter v2.6.1 and GAME_SOP v2.6.1. No inspected repository evidence requires npm semver to equal the Starter release number.

README now identifies GAME_SITE_STARTER v2.6.1 as the Starter template release and GAME_SOP v2.6.1 compatibility line. It explicitly identifies package version 0.1.0 as inherited private application/scaffold metadata, retained from the earlier baseline, and explains that this value does not identify the Starter release or SOP compatibility line. The ambiguous introductory “repository/package target” wording now reads “template release target.”

Neither package manifest nor lockfile was changed by this repair. No future versioning policy, versioning framework, new authority, or entrypoint was introduced.

The existing release-identity test now also checks `private: true`, agreement between package and both lockfile root versions, and README wording tied to the actual package version. Its original four-document current-release assertions remain. Release identity: **2 tests PASS**.

### P2-3 — Occurrence Classification And Wording Repair

The discovery search covered active source, docs, tests, historical docs, and the review's exact wording/markers. Classification is contextual; implementation identifiers are not renamed.

| Class | Occurrences and disposition |
|---|---|
| HISTORICAL_INTERNAL | All EntityDatabase references in `docs/STARTER_V2.5_CHANGE_PROPOSAL.md` (lines 93, 178, 379, 391, 736, 947), `docs/STARTER_2.0_ARCHITECTURE_PROPOSAL.md` (390–391), and the two dated `docs/superpowers/plans/2026-09-01-*` plans that name the component (71 and 106). Prior repair-report mentions remain historical evidence. Preserved. |
| ACTIVE_INTERNAL | Component imports, type annotations and calls in the four `src/pages/{heroes,weapons,items,maps}/[...path].astro` route files; `src/core/filter-state.ts`; the frontmatter of `src/components/EntityDatabase.astro`; and type references in `src/components/islands/EntityFilterIsland.tsx`. Current report references describe implementation evidence. Preserved. |
| PUBLIC_FACING | EntityDatabase's “Facts are generated from one validated module file.”, “Module index” / “Structured database”, and EntityFilterIsland's “records verified for the current fact dataset”. Removed the generation sentence while retaining the horizontal-scroll hint; labels now say “Browse” / “Database”; caption now says “details and patch versions”. |
| TEST_ONLY | EntityDatabase references in `tests/filter-state.test.ts`, `tests/media-rendering.test.ts`, and the expanded `tests/player-facing-metadata.test.ts`; forbidden-copy strings and internal evidence markers used by boundary tests. Retained as test evidence. |

There was no literal EntityDatabase name in the original visible copy: the public finding was implementation-oriented wording emitted by that component. The default Starter's intentional configuration tutorial and technical documentation retain relevant operator instructions. Those are distinct from accidental implementation descriptions in reusable player-facing entity UI.

Source changes are three static copy locations only. No architecture/class/module rename, data model, filter behavior, renderer structure, media-readiness code, route, feature flag, or publication authority changed.

The metadata test now includes EntityDatabase in the existing source-template guard and renders both hub and database views through the actual Astro component with its React island. Both rendered outputs retain the hero row, filtering control, hydration markup, and scrolling hint; neither contains the forbidden internal copy or evidence markers. This exercises the optional entity path even though the default build has entity modules disabled.

### Actual Validation Evidence

Intended runtime: Node v22.22.0. Initial default process: v22.16.0 at `/usr/local/bin/node`. All repair tests, validate, check, and build used `PATH=/Users/randyz/.nvm/versions/node/v22.22.0/bin:$PATH`; `node -v` returned v22.22.0 and `which node` returned `/Users/randyz/.nvm/versions/node/v22.22.0/bin/node`.

| Check | Actual result |
|---|---|
| Initial targeted RED run, before copy repair | 2 files; 4 failed / 4 passed. Release wording and source-template assertions exposed the findings. The two new render checks also exposed an over-literal test expectation for React text separators; the assertion was corrected to ignore HTML comments. |
| Targeted release/player-metadata/D1/D2 run | PASS: 6 files / 73 tests. Command: `npm test -- tests/release-identity.test.ts tests/player-facing-metadata.test.ts tests/media-readiness.test.ts tests/public-sources.test.ts tests/tool-definitions.test.ts tests/tool-island-boundary.test.ts`. |
| Release identity within that run | PASS: 2 tests. |
| Player-facing metadata within that run | PASS: 6 tests, including 2 actual entity render probes. |
| `npm test` | PASS: 35 files / 418 tests (previous baseline: 35 / 415; 3 assertions added as test cases). |
| `npm run validate` | PASS: 8 enabled pages / 1 content entry. |
| `npm run check` | PASS: Astro 118 files, 0 errors / 0 warnings / 0 hints; Vitest 35 files / 418 tests. Existing empty `src/content/meta` and `src/content/news` glob-loader notices remain outside Astro diagnostics. |
| `npm run build` | PASS: 8 pages built; 8 inventory routes reconciled; Pagefind indexed 4 pages; generated build audit passed for 8 pages. |
| Public output marker probe | PASS: all 41 fresh `dist` files scanned as bytes; zero matches for EntityDatabase, Facts are generated from one validated module file, Module index, current fact dataset, evidenceNote, INTERNAL_TOOL_EVIDENCE_LEAK_MARKER, and INTERNAL_EVIDENCE_NOTE_MARKER. |
| Page/output regression probe | PASS: the fresh 8 HTML paths exactly match the saved pre-repair output path set; build reconciliation, links/SEO/sitemap audit, and the full test suite pass. |
| D1 regression | PASS: existing media-readiness tests, including P1 repairs, remain passing; production media-readiness/parser and D1 tests are byte-identical to the pre-P2 snapshot. P2-1 remains untouched. |
| D2 regression | PASS: existing source projection, tool projection, and hydration boundary tests pass; player-facing metadata tests now also cover entity rendering. Existing D2 implementation is unchanged. |
| `git diff --check` | PASS. |

### Incremental Diff Classification

The comparison baseline is the dirty worktree captured immediately before this P2 repair, not HEAD. The aggregate Git diff also contains earlier authorized implementation and repair work.

| File changed by this P2 repair | Classification |
|---|---|
| `README.md` | P2_2_VERSION_IDENTITY |
| `src/components/EntityDatabase.astro` | P2_3_WORDING |
| `src/components/islands/EntityFilterIsland.tsx` | P2_3_WORDING |
| `tests/release-identity.test.ts` | TEST |
| `tests/player-facing-metadata.test.ts` | TEST |
| `V2.6.1_STARTER_IMPLEMENTATION_REPORT.md` | REPORT |

UNRELATED = 0; UNKNOWN = 0. All other pre-existing tracked/untracked file contents are preserved. Architecture drift = 0. SOP, Master Prompt, and previous review artifacts were not modified. No new files were added to the repository by this repair; the report was already untracked at entry.

### Current Status And Exact Next Step

**READY FOR FULL CROSS-REPO SEMANTIC RE-REVIEW**.

Next: run the separately authorized full cross-repo semantic re-review against the preserved P1 closure and this P2 repair evidence, carrying P2-1 as ACCEPTED / DEFERRED. This task does not start that review or release packaging. Full cross-repo review PASS and final Human Release Approval are not claimed.

HEAD remains `8f54c0844408e5eff4be2e456e88d11f68b69937`; staging remains empty. Commits = 0; pushes = 0; tags = 0; deployments = 0. No freeze or final release action was performed.
